# Socket in tmp/ keeps project root clean and avoids macOS 104-char path limit
OVERMIND_SOCKET=tmp/overmind.sock

# Don't auto-assign PORT=5000/5100/5200 — Procfile.dev uses explicit port vars
OVERMIND_NO_PORT=1

# Let Tailwind watcher exit without killing the whole stack
OVERMIND_CAN_DIE=css
