<%= kui(:app) do %>
  <div class="p-6">
    <h1 class="text-lg font-semibold">Application Content</h1>
    <p class="text-sm text-muted-foreground mt-1">
      The App component provides <code>bg-background</code>, <code>text-foreground</code>,
      and <code>antialiased</code> on the root wrapper. This ensures dark mode text
      colors inherit correctly.
    </p>
  </div>
<% end %>
