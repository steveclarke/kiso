<div class="flex gap-4 items-center text-foreground">
  <% %i[sm default lg].each do |size| %>
    <%= kui(:toggle, size: size, "aria-label": "Toggle bold (#{size})") do %>
      <%= kiso_icon("bold") %>
    <% end %>
  <% end %>
</div>
