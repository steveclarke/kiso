<div class="max-w-sm">
  <%= kui(:card, variant: variant) do %>
    <%= kui(:card, :header) do %>
      <%= kui(:card, :title) { title } %>
      <%= kui(:card, :description) { description } %>
    <% end %>
    <%= kui(:card, :content) do %>
      <p class="text-sm text-muted-foreground">
        Your card content goes here. Add any elements you need.
      </p>
    <% end %>
    <%= kui(:card, :footer, css_classes: "gap-2") do %>
      <%= kui(:button, variant: :outline, size: :sm) { "Cancel" } %>
      <%= kui(:button, size: :sm) { "Save" } %>
    <% end %>
  <% end %>
</div>
