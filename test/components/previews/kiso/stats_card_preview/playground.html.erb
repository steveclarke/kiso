<div class="max-w-xs">
  <%= kiso(:stats_card, variant: variant) do %>
    <%= kiso(:stats_card, :label) { label } %>
    <%= kiso(:stats_card, :value) { value } %>
    <%= kiso(:stats_card, :description) { description } %>
  <% end %>
</div>
