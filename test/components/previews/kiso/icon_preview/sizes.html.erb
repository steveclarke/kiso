<div class="flex gap-6 items-end p-8">
  <% %i[xs sm md lg xl].each do |size| %>
    <div class="flex flex-col items-center gap-2">
      <%= kiso_icon("lucide:heart", size: size) %>
      <span class="text-xs text-muted-foreground"><%= size %></span>
    </div>
  <% end %>
</div>
