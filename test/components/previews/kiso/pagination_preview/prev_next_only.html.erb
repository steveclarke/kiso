<%# Prev/Next only — no page numbers — developer builds a wrapper component like this %>
<%= kiso(:pagination) do %>
  <%= kiso(:pagination, :content) do %>
    <%= kiso(:pagination, :previous, href: "#") %>
    <%= kiso(:pagination, :next, href: "#") %>
  <% end %>
<% end %>
