<%# Last page: 1 ... 8 9 [10] — no next link %>
<%= kiso(:pagination) do %>
  <%= kiso(:pagination, :content) do %>
    <%= kiso(:pagination, :previous, href: "#") %>
    <%= kiso(:pagination, :item) { kiso(:pagination, :link, href: "#") { "1" } } %>
    <%= kiso(:pagination, :item) { kiso(:pagination, :ellipsis) } %>
    <%= kiso(:pagination, :item) { kiso(:pagination, :link, href: "#") { "8" } } %>
    <%= kiso(:pagination, :item) { kiso(:pagination, :link, href: "#") { "9" } } %>
    <%= kiso(:pagination, :item) { kiso(:pagination, :link, href: "#", active: true) { "10" } } %>
    <%= kiso(:pagination, :next, href: nil) %>
  <% end %>
<% end %>
