<div class="max-w-sm space-y-4">
  <%= kiso(:input, size: :sm, placeholder: "Small") %>
  <%= kiso(:input, size: :md, placeholder: "Medium (default)") %>
  <%= kiso(:input, size: :lg, placeholder: "Large") %>
</div>
