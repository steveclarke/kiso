<div class="max-w-sm">
  <%= kui(:field) do %>
    <%= kui(:field, :label, for: :feedback) { "Feedback" } %>
    <%= kui(:textarea, id: :feedback, name: :feedback, placeholder: "Tell us what you think...", rows: 4) %>
    <%= kui(:field, :description) { "Your feedback helps us improve. We read every message." } %>
  <% end %>
</div>
